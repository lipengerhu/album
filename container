<!DOCTYPE html>
<html>
<head>
    <title>测试页面</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
    <h1>测试页面正常</h1>
    <p>如果看到这个页面，说明部署成功</p>
    <div id="result"></div>

    <script>
        // 最简单的验证逻辑
        const urlParams = new URLSearchParams(window.location.search);
        const timestamp = urlParams.get('t');
        const signature = urlParams.get('s');
        
        const resultDiv = document.getElementById('result');
        
        if (!timestamp || !signature) {
            resultDiv.innerHTML = '<p style="color: red;">错误：缺少参数 t 或 s</p>';
        } else {
            resultDiv.innerHTML = `
                <p>参数接收成功：</p>
                <p>时间戳 t: ${timestamp}</p>
                <p>签名 s: ${signature}</p>
                <p>页面加载正常！</p>
            `;
        }
        
        console.log('页面加载完成');
        console.log('参数:', { timestamp, signature });
    </script>
</body>
</html>
