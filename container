<!DOCTYPE html>
<html>
<head>
    <title>测试页面</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
    <h1>测试页面正常</h1>
    <p>如果看到这个页面，说明部署成功</p>
    
    <script>
        // 最简单的验证逻辑
        const urlParams = new URLSearchParams(window.location.search);
        const timestamp = urlParams.get('t');
        const signature = urlParams.get('s');
        
        console.log('页面加载完成');
        console.log('参数:', { timestamp, signature });
        
        // 直接在body后面添加结果
        const resultHTML = !timestamp || !signature 
            ? '<p style="color: red;">错误：缺少参数 t 或 s</p>'
            : `
                <div style="margin-top: 20px; padding: 10px; background: #f5f5f5;">
                    <p>参数接收成功：</p>
                    <p>时间戳 t: ${timestamp}</p>
                    <p>签名 s: ${signature}</p>
                    <p>页面加载正常！</p>
                </div>
            `;
        
        document.body.innerHTML += resultHTML;
    </script>
</body>
</html>
