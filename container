<!DOCTYPE html>
<html>
<head>
    <title>测试页面</title>
    <meta charset="UTF-8">
</head>
<body>
    <h1>✅ 测试页面正常</h1>
    <p>如果看到这个页面，说明部署成功</p>
    
    <script>
        console.log('页面加载完成');
        const params = new URLSearchParams(window.location.search);
        console.log('URL参数:', Object.fromEntries(params));
        
        // 显示参数信息
        if (params.toString()) {
            document.body.innerHTML += `
                <div style="margin: 20px; padding: 15px; background: #e8f5e8; border-radius: 5px;">
                    <h3>URL参数信息：</h3>
                    <p>t: ${params.get('t') || '未提供'}</p>
                    <p>s: ${params.get('s') || '未提供'}</p>
                </div>
            `;
        }
    </script>
</body>
</html>
